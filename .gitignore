# ---------------------------------------------------------------------------
# 运行时生成 / 含敏感信息的文件 —— 绝对不要提交
# ---------------------------------------------------------------------------

# Grafana 管理员密码
.env

# BMC 账号密码(明文)。由 bmc-manager 网页生成和维护。
ipmi_exporter/ipmi.yml

# 被监控服务器的 IP 清单。由 bmc-manager 网页生成和维护。
prometheus/targets/bmc-targets.json

# 原子写入过程中的临时文件(正常情况下不会残留)
**/.tmp-*.swap

# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
.DS_Store
.snmp-creds
